# compiled binary
/ibmverify

# credentials — never commit these
keys.txt

# macOS
.DS_Store
demo.sh

# local secrets — never commit
.env
go.work
go.work.sum
demo.env.example
coverage.out
.bob/
dist/
